# pip packages for the spring-magnet-damping FEM project.
# Install in one shot:
#     pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
#
# Tested with Python 3.11 on Windows 10/11.

# Numerical backbone (required by simulate.py)
numpy>=2.0,<3.0
matplotlib>=3.8

# Optional - gmsh Python API (geom_preview.py / geom_preview.FCMacro)
gmsh>=4.13,<5.0

# Optional - .vtu / .msh I/O
meshio>=5.3,<6.0

# Optional - headless 3D rendering
pyvista>=0.43,<1.0

# Optional - MP4 / GIF stitching for animations
imageio>=2.30
